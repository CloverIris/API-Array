"use client";

import { Badge } from "@openai/apps-sdk-ui/components/Badge";
import { Button } from "@openai/apps-sdk-ui/components/Button";
import { Input } from "@openai/apps-sdk-ui/components/Input";
import { Select } from "@openai/apps-sdk-ui/components/Select";
import { Textarea } from "@openai/apps-sdk-ui/components/Textarea";
import { ApiKeys, CheckCircle, FileDocument, Key } from "@openai/apps-sdk-ui/components/Icon";
import { useEffect, useState } from "react";
import { getProviderCatalog, getProviderInstances, removeProviderInstance, runProviderProbe, storeSecret, upsertCustomProviderYaml, upsertProviderInstance, validateProviderYaml, type InspectionReport, type ProviderCatalogItem, type ProviderInstanceItem } from "../../lib/desktop";
import { EmptyPage, readError } from "../shared";

export function AssetsPage() {
  const [catalog, setCatalog] = useState<ProviderCatalogItem[]>([]);
  const [instances, setInstances] = useState<ProviderInstanceItem[]>([]);
  const [selectedId, setSelectedId] = useState("");
  const [instanceId, setInstanceId] = useState("");
  const [endpoint, setEndpoint] = useState("");
  const [secret, setSecret] = useState("");
  const [yaml, setYaml] = useState("");
  const [advanced, setAdvanced] = useState(false);
  const [report, setReport] = useState<InspectionReport | null>(null);
  const [message, setMessage] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  useEffect(() => { void Promise.all([getProviderCatalog(), getProviderInstances()]).then(([nextCatalog, nextInstances]) => { setCatalog(nextCatalog); setInstances(nextInstances); setSelectedId(nextCatalog[0]?.id ?? ""); }).catch((reason) => setMessage(readError(reason))); }, []);
  const selected = catalog.find((item) => item.id === selectedId);
  useEffect(() => { if (selected) setEndpoint(selected.defaultBaseUrl); }, [selected]);
  const secretReference = () => `secret://workspace/${instanceId.trim()}/api_key`;
  const save = async () => { if (!selected || !instanceId.trim() || !secret.trim()) { setMessage("请填写 Provider、实例名称和 API Key。保存后它会出现在 API 钱包中。"); return; } setBusy(true); setMessage(null); try { await storeSecret(secretReference(), secret); setSecret(""); setInstances(await upsertProviderInstance({ providerId: selected.id, instanceId: instanceId.trim(), endpointOverride: endpoint, enabled: true, secretFields: { api_key: secretReference() } })); setMessage("已存入 API 钱包。下一步可在 API 钱包卡片中直接使用，或加入 Canvas 编组。"); } catch (reason) { setMessage(readError(reason)); } finally { setBusy(false); } };
  const validate = async () => { setBusy(true); try { const result = await validateProviderYaml(yaml); setMessage(result.valid ? `YAML 有效：${result.name ?? result.providerId}` : result.error ?? "YAML 校验失败"); } catch (reason) { setMessage(readError(reason)); } finally { setBusy(false); } };
  const saveYaml = async () => { if (!yaml.trim() || !instanceId.trim()) { setMessage("请先填写实例名称和 YAML。"); return; } setBusy(true); try { await storeSecret(secretReference(), secret); setSecret(""); setInstances(await upsertCustomProviderYaml({ yaml, instanceId: instanceId.trim(), endpointOverride: endpoint, secretFields: { api_key: secretReference() } })); setMessage("自定义 Provider 已存入 API 钱包。"); } catch (reason) { setMessage(readError(reason)); } finally { setBusy(false); } };
  const inspect = async (id: string) => { setBusy(true); setMessage(null); try { setReport(await runProviderProbe(id)); } catch (reason) { setMessage(readError(reason)); } finally { setBusy(false); } };
  return <div className="assets-view reading-page-inner"><header className="page-heading"><div><p className="eyebrow">STEP 1 · STORE</p><h1>接入 API 到钱包</h1><p>这里的职责只有一个：把已有 API 的端点和 Key 安全保存起来。它不会创建 Publisher，也不会自动编组。</p></div><Badge color="info" variant="soft">{catalog.length} 个内置 Provider</Badge></header>{message ? <p className="error-message" role="alert">{message}</p> : null}<section className="station-explainer"><div><strong>1 · 接入 API</strong><span>保存端点与凭据</span></div><span className="station-arrow">→</span><div><strong>2 · 审计收费站</strong><span>单个 API 直接调用</span></div><span className="station-arrow">→</span><div><strong>3 · Canvas 编组</strong><span>多个 API 合成新端点</span></div></section><section className="form-card"><div className="form-grid"><label className="field-label">Provider<Select options={catalog.map((item) => ({ value: item.id, label: `${item.name} · ${item.adapter}` }))} value={selectedId} onChange={(option) => setSelectedId(option.value)} placeholder="选择 Provider" /></label><label className="field-label">钱包实例名称<Input value={instanceId} onChange={(event) => setInstanceId(event.target.value)} placeholder="例如 openai-main" /></label><label className="field-label">上游 Base URL<Input value={endpoint} onChange={(event) => setEndpoint(event.target.value)} disabled={!selected?.editableEndpoint} /></label><label className="field-label">上游 API Key<Input type="password" value={secret} onChange={(event) => setSecret(event.target.value)} placeholder="仅写入本机凭据存储" autoComplete="new-password" /></label></div><div className="form-actions"><Button color="primary" loading={busy} onClick={() => void save()}><Key />保存到 API 钱包</Button><Button color="secondary" variant="ghost" onClick={() => setAdvanced((value) => !value)}><FileDocument />{advanced ? "收起高级配置" : "使用 YAML 高级配置"}</Button></div>{advanced ? <div className="advanced-editor"><Textarea value={yaml} onChange={(event) => setYaml(event.target.value)} rows={10} placeholder="粘贴 Provider YAML；校验和保存后仍会进入 API 钱包。" /><div className="form-actions"><Button color="secondary" variant="soft" disabled={!yaml.trim()} loading={busy} onClick={() => void validate()}>校验 YAML</Button><Button color="primary" variant="soft" disabled={!yaml.trim() || !instanceId.trim()} loading={busy} onClick={() => void saveYaml()}>保存自定义 Provider</Button></div></div> : null}</section><section><div className="section-heading"><div><h2>已接入钱包的 Provider</h2><p className="page-subtitle">体检只验证上游能力，不会把它发布成新的 API。直接使用请回到 API 钱包。</p></div><span className="muted">{instances.length} 个</span></div>{instances.length ? <div className="asset-list">{instances.map((instance) => <article className="asset-card" key={instance.id}><div><div className="publisher-title"><ApiKeys className="size-4" /><h3>{instance.id}</h3><Badge color={instance.enabled ? "success" : "secondary"} variant="soft">{instance.enabled ? "已启用" : "已停用"}</Badge></div><p>{instance.name} · {instance.endpointOverride ?? "使用默认端点"}</p></div><div className="actions"><Button color="info" variant="soft" size="sm" loading={busy} onClick={() => void inspect(instance.id)}><CheckCircle />验证上游</Button><Button color="danger" variant="ghost" size="sm" onClick={() => void removeProviderInstance(instance.id).then(setInstances).catch((reason) => setMessage(readError(reason)))}>从钱包移除</Button></div></article>)}</div> : <EmptyPage page="钱包还没有 API" hint="选择 Provider、填写端点和 Key 后保存；然后回到 API 钱包开始直用或编组。" icon={ApiKeys} />}</section>{report ? <InspectionCard report={report} /> : null}</div>;
}

function InspectionCard({ report }: { report: InspectionReport }) { const color = report.overall === "healthy" ? "success" : report.overall === "degraded" ? "warning" : report.overall === "unavailable" ? "danger" : "secondary"; return <section className="inspection-card"><div className="section-heading"><div><p className="eyebrow">UPSTREAM CHECK</p><h2>上游验证报告 · {report.provider_id}</h2></div><Badge color={color} variant="soft" pill>{report.overall}</Badge></div><div className="finding-grid">{Object.values(report.findings).map((finding) => <article className="finding" key={finding.dimension}><div><strong>{finding.dimension}</strong><Badge color={finding.status === "passed" ? "success" : finding.status === "failed" ? "danger" : "secondary"} variant="soft">{finding.status}</Badge></div><p>{finding.safe_summary ?? "无附加说明"}</p>{finding.latency_ms !== undefined ? <small>{finding.latency_ms} ms</small> : null}</article>)}</div></section>; }
