"use client";

import { Badge } from "@openai/apps-sdk-ui/components/Badge";
import { Button } from "@openai/apps-sdk-ui/components/Button";
import { Input } from "@openai/apps-sdk-ui/components/Input";
import { Select } from "@openai/apps-sdk-ui/components/Select";
import { Textarea } from "@openai/apps-sdk-ui/components/Textarea";
import { CheckCircle, Copy, Pause, Play, Plugin, Plus } from "@openai/apps-sdk-ui/components/Icon";
import { useEffect, useState } from "react";
import { createDirectEndpoint, deleteDirectEndpoint, getDirectEndpointTemplates, getDirectEndpoints, getWalletGallery, pauseDirectEndpoint, startDirectEndpoint, testDirectEndpoint, updateDirectEndpoint, type CodeTemplate, type DirectEndpointItem, type DesktopSnapshot, type WalletCard } from "../../lib/desktop";
import { readError } from "../shared";

export function DirectEndpointsPage({ snapshot, initialAssetId }: { snapshot: DesktopSnapshot; initialAssetId?: string }) {
  const [assets, setAssets] = useState<WalletCard[]>([]); const [items, setItems] = useState<DirectEndpointItem[]>([]); const [selected, setSelected] = useState<DirectEndpointItem | null>(null); const [dialog, setDialog] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null); const [templates, setTemplates] = useState<CodeTemplate[]>([]); const [language, setLanguage] = useState("");
  const [assetId, setAssetId] = useState(initialAssetId ?? ""); const [name, setName] = useState(""); const [alias, setAlias] = useState(""); const [token, setToken] = useState(""); const [publicModel, setPublicModel] = useState("default"); const [upstreamModel, setUpstreamModel] = useState("default"); const [timeout, setTimeoutValue] = useState("30000"); const [retries, setRetries] = useState("2");
  const load = async () => { const [cards, endpoints] = await Promise.all([getWalletGallery(), getDirectEndpoints()]); const ready = cards.filter((card) => card.source === "asset"); setAssets(ready); setItems(endpoints); setAssetId((current) => current || initialAssetId || ready[0]?.id || ""); };
  useEffect(() => { void load().then(() => { const pending = sessionStorage.getItem("apiarray.direct.asset"); if (pending) { sessionStorage.removeItem("apiarray.direct.asset"); setAssetId(pending); setDialog(true); } }).catch((reason) => setError(readError(reason))); }, []);
  const reset = () => { setSelected(null); setName(""); setAlias(""); setToken(""); setPublicModel("default"); setUpstreamModel("default"); setTimeoutValue("30000"); setRetries("2"); };
  const openCreate = () => { reset(); setDialog(true); };
  const openEdit = (item: DirectEndpointItem) => { setSelected(item); setAssetId(item.assetId); setName(item.name); setAlias(item.alias); setToken(""); setPublicModel(item.publicModels[0] ?? "default"); setUpstreamModel(item.publicModels[0] ?? "default"); setDialog(true); };
  const save = async () => { if (!assetId || !name.trim() || !alias.trim() || (!selected && !token.trim())) { setError("请选择钱包资产，并填写名称、alias 和本地 Token。"); return; } setBusy(true); setError(null); try { const input = { endpointId: selected?.id, assetId, name, alias, token, publicModel, upstreamModel, timeoutMs: Number(timeout), maxRetries: Number(retries) }; setItems(selected ? await updateDirectEndpoint({ ...input, endpointId: selected.id }) : await createDirectEndpoint(input)); setDialog(false); reset(); } catch (reason) { setError(readError(reason)); } finally { setBusy(false); } };
  const toggle = async (item: DirectEndpointItem) => { setBusy(true); setError(null); try { setItems(item.enabled ? await pauseDirectEndpoint(item.id) : await startDirectEndpoint(item.id)); } catch (reason) { setError(readError(reason)); } finally { setBusy(false); } };
  const inspect = async (item: DirectEndpointItem) => { setBusy(true); setError(null); try { const result = await testDirectEndpoint(item.id); setError(result.safeSummary); } catch (reason) { setError(readError(reason)); } finally { setBusy(false); } };
  const docs = async (item: DirectEndpointItem) => { setSelected(item); const next = await getDirectEndpointTemplates(item.id); setTemplates(next); setLanguage(next[0]?.language ?? ""); };
  const remove = async (item: DirectEndpointItem) => { if (!confirm(`删除直出端点“${item.name}”？钱包资产不会被删除。`)) return; setItems(await deleteDirectEndpoint(item.id)); };
  const current = templates.find((template) => template.language === language) ?? templates[0];
  return <div className="reading-page-inner direct-page"><header className="page-heading"><div><p className="eyebrow">AUDITED DIRECT OUTPUT</p><h1>怎样把某个 API 经审计后直接使用？</h1><p>从钱包选择资产，创建独立 alias、Token 和模型映射。同一个钱包 API 可以创建多个隔离端点。</p></div><Button color="primary" disabled={!assets.length} onClick={openCreate}><Plus />创建审计直出</Button></header><section className="gateway-summary"><div><strong>统一 LocalGateway</strong><code>{snapshot.gateway.baseUrl}</code></div><Badge color={snapshot.gateway.running ? "success" : "warning"} variant="soft">{snapshot.gateway.running ? `${snapshot.gateway.entryCount} 个入口` : "未运行"}</Badge></section>{error ? <p className={error.includes("通过") ? "success-message" : "error-message"} role="alert">{error}</p> : null}{!assets.length ? <div className="empty-state"><Plugin /><strong>钱包还没有可用 API</strong><p>先到 API 钱包添加并验证上游，再回来创建审计直出端点。</p></div> : null}<div className="direct-endpoint-grid">{items.map((item) => <article className="direct-endpoint-card" key={item.id}><div><div className="publisher-title"><Plugin /><h2>{item.name}</h2><Badge color={item.enabled ? "success" : "secondary"} variant="soft">{item.enabled ? "运行中" : "已暂停"}</Badge></div><p>{item.assetName} → {item.baseUrl}</p><small>{item.publicModels.join("、")} · {item.requestCount} 次审计调用</small></div><div className="actions"><Button color={item.enabled ? "warning" : "success"} variant="soft" size="sm" disabled={busy} onClick={() => void toggle(item)}>{item.enabled ? <Pause /> : <Play />}{item.enabled ? "暂停" : "启动"}</Button><Button color="secondary" variant="ghost" size="sm" disabled={!item.enabled || busy} onClick={() => void inspect(item)}><CheckCircle />自检</Button><Button color="secondary" variant="ghost" size="sm" onClick={() => void navigator.clipboard.writeText(item.baseUrl)}><Copy />复制 URL</Button><Button color="secondary" variant="ghost" size="sm" onClick={() => void docs(item)}>模板</Button><Button color="secondary" variant="ghost" size="sm" onClick={() => openEdit(item)}>编辑</Button><Button color="danger" variant="ghost" size="sm" onClick={() => void remove(item)}>删除</Button></div></article>)}</div>{current ? <section className="templates-view"><div className="template-toolbar"><Select value={language} options={templates.map((template) => ({ value: template.language, label: template.title }))} onChange={(option) => setLanguage(option.value)} /><Button color="secondary" variant="soft" onClick={() => void navigator.clipboard.writeText(current.code)}><Copy />复制代码</Button></div><Textarea className="code-template" readOnly rows={18} value={current.code} /></section> : null}{dialog ? <div className="wallet-dialog" role="dialog" aria-modal="true" aria-label="审计直出配置"><div><Button className="dialog-close" color="secondary" variant="ghost" uniform aria-label="关闭" onClick={() => setDialog(false)}>×</Button><p className="eyebrow">DIRECT ENDPOINT</p><h2>{selected ? "编辑审计直出" : "创建审计直出"}</h2><label className="field-label">来源钱包 API<Select value={assetId} options={assets.map((asset) => ({ value: asset.id, label: asset.name }))} onChange={(option) => setAssetId(option.value)} /></label><label className="field-label">端点名称<Input value={name} onChange={(event) => { setName(event.target.value); if (!selected) setAlias(event.target.value.toLowerCase().replace(/[^a-z0-9_-]+/g, "-")); }} /></label><label className="field-label">稳定 alias<Input value={alias} onChange={(event) => setAlias(event.target.value)} /></label><div className="form-grid"><label className="field-label">公开模型名<Input value={publicModel} onChange={(event) => setPublicModel(event.target.value)} /></label><label className="field-label">上游模型名<Input value={upstreamModel} onChange={(event) => setUpstreamModel(event.target.value)} /></label><label className="field-label">超时 ms<Input type="number" value={timeout} onChange={(event) => setTimeoutValue(event.target.value)} /></label><label className="field-label">重试次数<Input type="number" min="0" max="10" value={retries} onChange={(event) => setRetries(event.target.value)} /></label></div><label className="field-label">本地访问 Token<Input type="password" autoComplete="new-password" value={token} onChange={(event) => setToken(event.target.value)} placeholder={selected ? "留空表示不替换" : "与上游 Key 完全独立"} /></label><Button color="primary" block loading={busy} onClick={() => void save()}>{selected ? "保存直出配置" : "创建直出端点"}</Button></div></div> : null}</div>;
}
